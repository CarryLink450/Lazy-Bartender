Asset replacement notes:

- Replace `logo.png` with the real business logo. The app already points the header, hero, footer, and favicon to `/logo.png`.
- Replace `cocktail-hero.png` with a final hero photo if desired.
- Add real cocktail and event photos later, then update the Gallery component in `src/App.jsx`.
